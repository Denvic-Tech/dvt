
# Sentinel-значение для "значение не передано", аналог `undefined`.
# Используется для того, чтобы отличать явно переданый null.
# Literal тип `UnsetType` находится в `src/types/common.py`
UNSET = "__DVT_UNSET__"
