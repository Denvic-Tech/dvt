# Установка часового пояса

Тип ноды: `DataFrameSetTimezone`.

## Назначение и выбор

Назначает пояс датам без пояса или переводит даты с поясом в другой.

## Входы и настройка

Подключите `df`; задайте `column` и корректное имя `timezone` (по умолчанию `Europe/Moscow`).

## Результат

`output` заменяет выбранную колонку датами с часовым поясом.

## Поведение и ограничения

Вход другого типа разбирается как дата, неверные значения становятся NaT. При назначении пояса наивной дате её часы сохраняются. При переводе даты с поясом сохраняется момент времени. Неоднозначное или несуществующее местное время при локализации становится NaT.

## Примеры

Вход — дата с поясом `2026-01-01 00:00:00+00:00`.

Значения параметров, без оболочки MCP patch:

```json
{
  "column": "event_at",
  "timezone": "Europe/Moscow"
}
```

Результат — `2026-01-01 03:00:00+03:00`. Полночь без пояса, напротив, стала бы московской полуночью.

## Типовые ошибки

Неверное смещение: различайте назначение и перевод пояса. Если наивные даты означают UTC, сначала назначьте UTC, затем переведите. Проверьте имя пояса и переходы времени.
