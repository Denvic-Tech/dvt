# Замена по регулярному выражению

Тип ноды: `DataFrameRegexReplace`.

## Назначение и выбор

Заменяет совпадающий текст в одной колонке по регулярному выражению.

## Входы и настройка

Подключите `df`; задайте `column_to_replace`, `pattern` и `replacement`. Замена по умолчанию — пустая строка, то есть удаление совпадений.

## Результат

`output` сохраняет форму DataFrame и заменяет значения выбранной колонки.

## Поведение и ограничения

Перед regex-заменой колонка приводится через `astype(str)`. Числа и представления пропусков становятся текстом; это не числовое преобразование с сохранением null. Базовая строковая операция обрабатывает все совпадения.

## Примеры

Входное `code` равно `AB-123`.

Значения параметров (без оболочки MCP patch):

```json
{
  "column_to_replace": "code",
  "pattern": "[^0-9]",
  "replacement": ""
}
```

Выходное значение — строка `123`.

## Типовые ошибки

Некорректный regex: исправьте выражение и экранирование JSON. Нужен другой тип результата: добавьте явное приведение после замены. Передавайте строковую замену, а не null.
